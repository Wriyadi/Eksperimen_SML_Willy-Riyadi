global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'ml_model_metrics'
    static_configs:
      - targets: ['host.docker.internal:8000'] # Gunakan localhost:8000 jika Prometheus diinstal tanpa Docker